name: ci-supporters-portal-ui

on:
  pull_request:
    types: [opened, reopened, edited, synchronize]
    paths:
      - "supporters/src/UI/**"
      - ".github/workflows/ci-supporters-portal-ui.yml"

env:
  IMAGE_NAME: supporters-portal-ui

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build image
        working-directory: ./supporters/src/UI
        run: docker build . --file Dockerfile --tag $IMAGE_NAME
